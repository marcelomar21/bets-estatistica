---
description: Performs a dry run of the daily pipeline to see what steps would be executed without actually running them.
---

Perform a dry run of the pipeline:

// turbo
1. Execute pipeline in dry-run mode
```bash
node scripts/pipeline.js --dry-run
```
