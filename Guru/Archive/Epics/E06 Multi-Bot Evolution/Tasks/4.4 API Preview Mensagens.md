---
status: todo
phase: 4
priority: medium
epic: '[[_Overview|E06]]'
files:
- admin-panel/src/app/api/bets/post-now/preview/route.ts
depends_on:
- '[[4.3 Integrar Tom copyService]]'
tags:
- task
- epic/e06
- phase/4
permalink: guru/epics/e06-multi-bot-evolution/tasks/4.4-api-preview-mensagens
---

# 4.4 API endpoint para preview de mensagens

## Objetivo

Criar endpoint que gera previews de mensagens sem enviar ao Telegram, persistindo-os em tabela dedicada para suportar edicao e regeneracao.

## Action

1. `POST /api/bets/post-now/preview` — recebe `{ group_id }`, retorna `{ previewId, bets: [{ betId, preview: string, betInfo: {...} }] }`
2. Internamente: chama mesma logica de `getFilaStatus` + `generateBetCopy` (com toneConfig do grupo)
3. Textos sao gerados mas NAO enviados ao Telegram
4. `previewId` e textos gerados sao persistidos na tabela **`post_previews`** no Supabase (nao in-memory):
   ```sql
   CREATE TABLE post_previews (
     id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
     preview_id TEXT NOT NULL UNIQUE,
     group_id UUID NOT NULL REFERENCES groups(id),
     user_id UUID NOT NULL REFERENCES auth.users(id),
     bets JSONB NOT NULL,           -- [{ betId, preview, betInfo, overrideText? }]
     status TEXT DEFAULT 'draft',   -- 'draft' | 'confirmed' | 'expired'
     created_at TIMESTAMPTZ DEFAULT now(),
     expires_at TIMESTAMPTZ DEFAULT (now() + interval '30 minutes')
   );
   CREATE INDEX idx_post_previews_lookup ON post_previews (preview_id) WHERE status = 'draft';
   ```
5. Endpoint `POST /api/bets/post-now/preview/regenerate` — recebe `{ previewId, betId }`, regenera copy de 1 bet, retorna texto novo + **diff client-side** (ver Task 4.5)
6. **Auth**: usar `createApiHandler` + tenant middleware (mesmo padrao de `post-now/route.ts`). Validar que o user tem acesso ao `group_id`

## Files

| Arquivo | Descricao |
|---------|-----------|
| `admin-panel/src/app/api/bets/post-now/preview/route.ts` | Novo — preview de mensagens sem envio |

## Acceptance Criteria

- [ ] Endpoint retorna previews gerados para todas as bets do grupo
- [ ] Previews sao persistidos na tabela `post_previews` com TTL de 30min
- [ ] Endpoint de regenerate regenera copy de 1 bet e retorna texto novo
- [ ] Auth valida que o user tem acesso ao `group_id`

## Notes

- Persistir no Supabase em vez de cache in-memory resolve:
  - (a) Vercel serverless pode escalar/reiniciar sem perder estado
  - (b) Concorrencia entre admins isolada por `preview_id`
  - (c) TTL via `expires_at` com cleanup por cron ou trigger SQL
- Adicionar esta migration como `sql/migrations/033_post_previews.sql`.
- Preview/edicao acontece **so no admin panel web**; Telegram mantem fluxo read-only (Confirmar/Cancelar) como fallback.

## Contexto

- [[Codebase Patterns]]
- [[4.3 Integrar Tom copyService]] — fornece `generateBetCopy` com tone
- [[4.5 UI Preview Edicao]] — consome este endpoint
- [[4.6 Overrides Post-Now]] — usa `post_previews` para overrides

## Log de Implementacao

_Vazio — preencher durante o desenvolvimento._