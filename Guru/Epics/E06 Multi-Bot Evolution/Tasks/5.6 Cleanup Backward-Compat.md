---
status: todo
phase: 5
priority: medium
epic: "[[_Overview|E06]]"
files:
  - lib/config.js
  - bot/telegram.js
depends_on:
  - "[[5.1 Server Multi-Webhook]]"
  - "[[5.2 Scheduler Factory]]"
  - "[[5.3 Propagar botCtx Handlers]]"
  - "[[5.4 Propagar groupId Jobs]]"
  - "[[5.5 Propagar botCtx Services]]"
tags: [task, epic/e06, phase/5]
---

# 5.6 Remover backward-compat e cleanup

## Objetivo

Remover todo o codigo de backward-compatibility temporario e env vars per-bot, consolidando a arquitetura multi-bot.

## Action

1. Remover fallback env vars de `telegram.js` (o warning de backward-compat da Task 2.1)
2. Remover env vars per-bot da `validateConfig()` (`TELEGRAM_BOT_TOKEN`, `TELEGRAM_ADMIN_GROUP_ID`, `TELEGRAM_PUBLIC_GROUP_ID`, `GROUP_ID`)
3. Remover fallback `config.telegram.publicGroupId` de `kick-expired.js`, `webhookProcessors.js`, `startCommand.js`
4. `BOT_MODE` env var removida (processo unico roda tudo)
5. **Deduplicacao de jobs centrais**: com a remocao de `BOT_MODE`, garantir que jobs cross-group (`distributeBets`, `trackResults`, `enrichOdds`) rodam **exatamente 1 vez** no processo unificado (nao N vezes, uma por scheduler). Estrategia: manter jobs centrais no `server.js` como crons globais unicos (fora dos schedulers per-group). Schedulers per-group gerenciam apenas: posting jobs, renewalReminders, syncMembers.

## Files

| Arquivo | Descricao |
|---------|-----------|
| `lib/config.js` | Remover env vars per-bot de validateConfig |
| `bot/telegram.js` | Remover fallback backward-compat |

## Acceptance Criteria

- [ ] **AC 5.5**: Given o cleanup executado, when `grep -rn "config\.telegram\.adminGroupId\|config\.telegram\.publicGroupId" bot/ lib/` e executado, then zero matches sao encontrados.
- [ ] Script `scripts/lint-no-singleton-config.sh` existe e falha se encontrar referencias deprecated
- [ ] Jobs centrais rodam exatamente 1 vez no processo unificado

## Notes

- So executar depois que o deploy unificado estiver validado.
- A separacao jobs centrais (global) vs jobs per-group (scheduler) e critica para evitar N distribuicoes ou N trackings simultaneos.
- **Verificacao automatizada**: adicionar script `scripts/lint-no-singleton-config.sh` que roda no CI e falha se encontrar referencias deprecated.

## Contexto

- [[Codebase Patterns]]
- [[5.1 Server Multi-Webhook]] ate [[5.5 Propagar botCtx Services]] — todas as tasks anteriores devem estar completas

## Log de Implementacao

_Vazio — preencher durante o desenvolvimento._
