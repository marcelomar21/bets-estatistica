---
status: todo
phase: 4
priority: medium
epic: "[[_Overview|E06]]"
files:
  - admin-panel/src/app/api/bets/post-now/route.ts
depends_on:
  - "[[4.4 API Preview Mensagens]]"
tags: [task, epic/e06, phase/4]
---

# 4.6 Suporte a overrides no post-now

## Objetivo

Modificar o endpoint post-now para aceitar textos editados do fluxo de preview, garantindo que mensagens customizadas pelo operador sejam enviadas ao Telegram.

## Action

1. Aceitar campo opcional `previewId: string` no body (alem do existente `group_id`)
2. Se `previewId` presente: ler overrides da tabela `post_previews` (status='draft', nao expirado), extrair textos editados do campo `bets[].overrideText`
3. Passar overrides ao bot: setar `post_now_requested_at` + gravar `preview_id` referencia na tabela `groups` (campo `active_preview_id TEXT`)
4. Bot ao detectar `post_now_requested_at`: se `active_preview_id` existe, le `post_previews` e usa textos editados; senao, gera via LLM como hoje
5. Apos postagem: bot marca `post_previews.status = 'confirmed'` e limpa `groups.active_preview_id`

## Files

| Arquivo | Descricao |
|---------|-----------|
| `admin-panel/src/app/api/bets/post-now/route.ts` | Aceitar `previewId` e ler overrides da tabela `post_previews` |

## Acceptance Criteria

- [ ] **AC 4.4**: Given operador edita o texto de uma mensagem no preview, when confirma e envia, then o Telegram recebe o texto editado (nao o original gerado por LLM).
- [ ] Post-now sem `previewId` continua funcionando como antes (backward-compat)

## Notes

- Nao usa `pending_post_overrides` JSONB em `groups` (race condition se 2 admins postam simultaneamente).
- A tabela `post_previews` isola cada sessao por `preview_id`.
- Requer migration `033_post_previews.sql` (criada na Task 4.4).
- Concorrencia: `post_previews` tabela no Supabase com `preview_id` unico por sessao; `active_preview_id` em `groups` garante que so 1 postagem e processada por vez.

## Contexto

- [[Codebase Patterns]]
- [[4.4 API Preview Mensagens]] — cria a tabela `post_previews`
- [[4.5 UI Preview Edicao]] — envia os overrides via este endpoint

## Log de Implementacao

_Vazio — preencher durante o desenvolvimento._
