---
status: todo
phase: 5
priority: medium
epic: '[[_Overview|E06]]'
files:
- bot/services/alertService.js
- bot/services/betService.js
- bot/services/copyService.js
depends_on:
- '[[5.1 Server Multi-Webhook]]'
tags:
- task
- epic/e06
- phase/5
permalink: guru/epics/e06-multi-bot-evolution/tasks/5.5-propagar-bot-ctx-services-1
---

# 5.5 Propagar botCtx para services

## Objetivo

Services que chamam `sendToAdmin`/`sendToPublic` passam a receber `botCtx`, garantindo que alertas sao enviados ao grupo correto.

## Action

1. Services que chamam `sendToAdmin`/`sendToPublic` recebem `botCtx`
2. `betService` ja filtra por `groupId` na maioria dos metodos — auditar e garantir cobertura
3. `copyService` ja recebe `toneConfig` (Task 4.3) — agora tambem recebe `botCtx` completo

Services a modificar:
- `bot/services/alertService.js` — Alertas com `sendToAdmin` hardcoded -> add botCtx param
- `bot/services/betService.js` — Auditar filtro por groupId em todos os metodos
- `bot/services/copyService.js` — Receber botCtx completo (alem de toneConfig)

## Files

| Arquivo | Descricao |
|---------|-----------|
| `bot/services/alertService.js` | sendToAdmin hardcoded -> recebe botCtx |
| `bot/services/betService.js` | Auditar e garantir filtro por groupId |
| `bot/services/copyService.js` | Recebe botCtx completo |

## Acceptance Criteria

- [ ] `alertService` envia alertas ao grupo correto via `botCtx`
- [ ] `betService` filtra por `groupId` em todos os metodos
- [ ] `copyService` recebe `botCtx` e usa `toneConfig` do grupo correto

## Notes

- `copyService` ja recebe `toneConfig` (Task 4.3) — agora tambem recebe `botCtx` completo.
- `alertService.js` usa `sendToAdmin` hardcoded atualmente.
- `betService` ja filtra por `groupId` na maioria dos metodos — precisa audit completo.

## Contexto

- [[Codebase Patterns]]
- [[5.1 Server Multi-Webhook]]
- [[4.3 Integrar Tom copyService]] — copyService ja recebe toneConfig

## Log de Implementacao

_Vazio — preencher durante o desenvolvimento._