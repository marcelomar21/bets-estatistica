---
status: todo
phase: 2
priority: high
epic: "[[_Overview|E06]]"
files:
  - sql/migrations/029_bot_pool_source_of_truth.sql
  - sql/migrations/030_group_config_columns.sql
  - sql/migrations/031_bet_result_confidence.sql
  - sql/migrations/032_tracking_recovery_index.sql
depends_on: []
tags: [task, epic/e06, phase/2]
---

# 2.3 Migrations SQL para campos novos (separadas por concern)

## Objetivo

Criar as migrations SQL necessarias para suportar as novas funcionalidades: bot_pool como source of truth, config per-group, result confidence, e indice para recovery sweep.

## Action

1. **029 — bot_pool como source of truth**: Garantir que tabela `bot_pool` tem explicitamente: `id SERIAL PRIMARY KEY`, `group_id UUID NOT NULL REFERENCES groups(id)`, `bot_token TEXT NOT NULL`, `admin_group_id BIGINT NOT NULL`, `public_group_id BIGINT NOT NULL`, `is_active BOOLEAN DEFAULT true`, `created_at TIMESTAMPTZ DEFAULT now()`. Se colunas `admin_group_id` / `public_group_id` nao existem, fazer `ALTER TABLE bot_pool ADD COLUMN`. Deprecar `groups.bot_token` — `bot_pool` e a **unica fonte de verdade** para tokens e IDs de chat do Telegram. Adicionar comment: `COMMENT ON TABLE bot_pool IS 'Source of truth para tokens e chat IDs do Telegram. groups.bot_token esta deprecated.'`

2. **030 — config per-group**: `ALTER TABLE groups ADD COLUMN max_active_bets INTEGER DEFAULT NULL` + `ALTER TABLE groups ADD COLUMN copy_tone_config JSONB DEFAULT '{}'::jsonb`

3. **031 — result_confidence**: `ALTER TABLE suggested_bets ADD COLUMN result_confidence TEXT CHECK (result_confidence IN ('high', 'medium', 'low'))`

4. **032 — index para recovery sweep**: `CREATE INDEX idx_bets_tracking_recovery ON suggested_bets (bet_status, bet_result, kickoff_time) WHERE bet_status = 'posted' AND bet_result = 'pending'` — indice parcial otimiza a query do recovery sweep

## Files

| Arquivo | Descricao |
|---------|-----------|
| `sql/migrations/029_bot_pool_source_of_truth.sql` | bot_pool como source of truth para tokens e chat IDs |
| `sql/migrations/030_group_config_columns.sql` | max_active_bets e copy_tone_config per-group |
| `sql/migrations/031_bet_result_confidence.sql` | result_confidence na suggested_bets |
| `sql/migrations/032_tracking_recovery_index.sql` | Indice parcial para recovery sweep |

## Acceptance Criteria

- [ ] Tabela `bot_pool` tem colunas `admin_group_id`, `public_group_id`, `is_active`
- [ ] Tabela `groups` tem colunas `max_active_bets` e `copy_tone_config`
- [ ] Tabela `suggested_bets` tem coluna `result_confidence` com CHECK constraint
- [ ] Indice parcial `idx_bets_tracking_recovery` criado

## Notes

- Migrations separadas por concern. Cada uma e independente e pode ser aplicada/revertida isoladamente.
- Aplicar via Supabase Management API.
- `bot_pool` e a source of truth para tokens — ao construir `BotContext`, ler de `bot_pool` (nao de `groups.bot_token`).
- Pode rodar em paralelo com Task 2.1.

## Contexto

- [[Codebase Patterns]]
- Supabase Management API para aplicar migrations
- Supabase project ref: `vqrcuttvcgmozabsqqja`

## Log de Implementacao

_Vazio — preencher durante o desenvolvimento._
